<div class="row">
  <div class="col-md-offset-1 col-md-10">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3>Memoryize! <%= @first.book %> <%= @first.chapter %>:<%=  @first.verse %></h3>
      </div>
      <div class="panel-body" id="first">

        <%= @first.text %>
        <div class="first"><%= @firsts.html_safe %></div>
        <p>
          Print this out and keep in your pocket! 
        </p>
      </div>
    </div>
  </div>
</div>
